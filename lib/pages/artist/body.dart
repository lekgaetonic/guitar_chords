import 'package:flutter/material.dart';

class ArtistBody extends StatelessWidget {
  const ArtistBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Artist'),
    );
  }
}
